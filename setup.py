from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='A modified Keras implementation of ResNet residual neural network with bottleneck block architecture (2019).',
    author='ansara',
    license='MIT',
)
